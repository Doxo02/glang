import("stdlib/linux");

fn strlen(str: char*) -> i64 {
    let c: char = *str;
    let i: i64 = 0;
    while(c != 0) {
        c = *(str + i);
        i = i + 1;
    }
    return i-1;
}

fn puts(str: char*) -> void {
    sys_write(STDOUT, str, strlen(str));
}